# /a11y-audit - WCAG 접근성 종합 감사

## Purpose
MindLog UI의 WCAG 2.1 AA 준수 현황을 2-agent 병렬 감사로 검증.
Semantics 커버리지, AppAccessibility 활용률, 터치 영역 크기, 색상 대비 패턴을 자동 점검.

## Usage
```
/a11y-audit           ← 2-agent 병렬 전체 감사 (기본)
/a11y-audit [path]    ← 특정 경로만 감사 (기본값: lib/presentation/)
/a11y-audit status    ← Semantics 사용률 빠른 통계만 출력
```

## Arguments
- `path` (optional) — 감사 대상 경로 (기본값: `lib/presentation/`)

## 트리거 조건
- 새 화면/위젯 추가 후
- 접근성 관련 버그 수정 후
- 배포 전 WCAG 준수 검증
- AppAccessibility 클래스 수정 후

---

## 실행 방식 (2-Agent 병렬)

```
/a11y-audit 실행 시:
┌───────────────────────────────────────────────┐
│                   a11y-audit                   │
│                                               │
│  ┌──────────────────┐  ┌───────────────────┐  │
│  │ semantics-auditor│  │  wcag-checker     │  │
│  │                  │  │                   │  │
│  │ Semantics 커버리지│  │ 터치 영역 크기    │  │
│  │ AppAccessibility │  │ 색상 대비 패턴    │  │
│  │ 이모지 레이블    │  │ 이미지 alt-text   │  │
│  │ 화면 namesRoute  │  │ 텍스트 잘림 탐지  │  │
│  └──────────────────┘  └───────────────────┘  │
│           ↓                    ↓               │
│              통합 감사 리포트 생성               │
└───────────────────────────────────────────────┘
```

에이전트 파일:
- `.claude/agents/a11y-audit/semantics-auditor.md`
- `.claude/agents/a11y-audit/wcag-checker.md`

---

## 에이전트별 담당 영역

### semantics-auditor
- 탭 가능 위젯(GestureDetector/InkWell/IconButton) Semantics 누락 탐지
- AppAccessibility 유틸 활용률 점검 (현재 0건 사용 — dead code 경고)
- 이모지/Icon 대체 레이블 누락 탐지
- AccessibilityWrapper 화면 수준 커버리지 확인
- excludeSemantics 오남용 탐지

### wcag-checker
- 터치 영역 44dp 미달 탐지 (WCAG 2.5.5)
- 하드코딩 색상 리터럴 탐지 → 다크모드 대비 위험 (WCAG 1.4.3)
- 이미지 semanticLabel/alt-text 누락 (WCAG 1.1.1)
- 텍스트 잘림 패턴(maxLines+clip) → 텍스트 크기 조정 위험 (WCAG 1.4.4)
- 저명도 텍스트 색상 패턴 (alpha < 0.4)

---

## `status` — 빠른 현황
출력 예시:
```
📊 A11y Status (2026-02-27)
  Semantics 사용 파일: 7 / ~90개 위젯 파일 (7.8%)
  AppAccessibility 활용: 0개 파일 (유틸 dead code 상태)
  AccessibleIconButton: 0건 사용
  → /a11y-audit 전체 감사 권장
```

---

## WCAG 기준 요약

| 항목 | 기준 | MindLog 상태 |
|------|------|-------------|
| 텍스트 대비 | 4.5:1 (AA) | darkTextColor 충족 ✅ |
| 터치 영역 | 44dp 이상 | 미검증 |
| 이미지 alt-text | 의미있는 이미지 필수 | 미검증 |
| Semantics 커버리지 | 탭 위젯 전체 | 7파일/~90개 (낮음) |
| 텍스트 크기 조정 | 200% 지원 | 미검증 |

---

## 접근성 유틸 현황 (`lib/core/accessibility/app_accessibility.dart`)

| 유틸 | 용도 | 현재 사용 |
|------|------|----------|
| `AppAccessibility.emotionScoreLabel()` | 감정 점수 → 스크린리더 레이블 | ❌ 0건 |
| `AppAccessibility.emotionEmojiLabel()` | 이모지 → 텍스트 설명 | ❌ 0건 |
| `AppAccessibility.diaryItemLabel()` | 일기 항목 전체 레이블 | ❌ 0건 |
| `AppAccessibility.dateLabel()` | 날짜 → 상대적 표현 | ❌ 0건 |
| `AppAccessibility.buttonHint()` | 버튼 힌트 문구 | ❌ 0건 |
| `AppAccessibility.analysisStatusLabel()` | 분석 상태 설명 | ❌ 0건 |
| `AccessibleIconButton` | 접근성 아이콘 버튼 | ❌ 0건 |
| `AccessibleCard` | 접근성 카드 위젯 | ❌ 0건 |
| `AccessibilityAnnouncer` | 동적 상태 알림 | ❌ 0건 |

---

## 출력 형식 (audit)

```markdown
# A11y Audit Report

## Executive Summary
| 영역 | 상태 | Critical | High | Medium |
|------|------|----------|------|--------|
| Semantics 커버리지 | PASS/WARN/FAIL | 0 | 0 | 0 |
| WCAG 준수 | PASS/WARN/FAIL | 0 | 0 | 0 |

## 즉시 조치 필요 (High)
...

## 상세 에이전트 리포트
[semantics-auditor 결과]
[wcag-checker 결과]

## 다음 권장 액션
1. 미사용 AppAccessibility 유틸 UI에 연결
   → DiaryItemCard: AppAccessibility.diaryItemLabel() 활용
   → SentimentDashboard: AppAccessibility.emotionScoreLabel() 활용
2. 터치 영역 미달 위젯 44dp 이상으로 확장
3. 이모지 Text 위젯 → AccessibleEmotionIndicator 또는 Semantics 감싸기
```

## 관련 스킬
- `/l10n-manager audit` — 다국어 키 감사 (텍스트 내용 완전성)
- `/swarm-review [path]` — 전체 코드 품질 감사 (아키텍처/보안/성능)
- `/color-migrate [file]` — 하드코딩 색상 → 테마 색상 마이그레이션
